from bs4 import BeautifulSoup
import requests
url = "https://en.wikipedia.org/wiki/List_of_largest_companies_in_the_United_States_by_revenue "

headers = {
   "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
}
page = requests.get(url, headers= headers)
soup = BeautifulSoup(page.text,"html.parser")
#print(soup)

#soup.find('table')

company_table = soup.find_all("table", class_="wikitable")[1]
#print(company_table)
#<table class="wikitable sortable jquery-tablesorter">
#<caption>
tablehead = soup.find_all('th')
print(tablehead)

table = soup.find('table')
world_titles = table.find_all('th')
world_titles

world_table_titles = [title.text.strip() for title in world_titles]
print(world_table_titles)

import pandas as pd
df = pd.DataFrame(columns = world_table_titles)
df

column_data = table.find_all('tr')
for row in column_data[1:]:
    row_data = row.find_all('td')
    individual_row_data = [data.text.strip() for data in row_data]
    #print(individual_row_data)

    lenght = len(df)
    df.loc[lenght] = individual_row_data

df

#we can export the code in panda to a csv file, we just need to copy and paste the folder path
df.to_csv(r'C:\Users\tobzi\Documents\Tobzid Jobs App\Industry CV-Supply Chain\CodelearnFiles.csv', index = False)
